# Lint gate for root/usr/lib/smart_srun/ (see .github/workflows/ci.yml).
#
# Keep the accepted rule set explicit and the tool version pinned in CI.
# Change both deliberately so local and CI runs use a reproducible lint gate.
#
# Deliberately not enabled yet, with the reason:
#   UP031 percent formatting -- mechanical style changes are a separate task.
#   BLE001 blind except -- the daemon catches broadly on purpose.
#   I001 import sorting -- modules import bare names off a sys.path
#                entry (see CLAUDE.md); resorting imports is not free here.

[lint]
select = ["E4", "E7", "E9", "F"]
